import java.util.ArrayList;

/**
     * 当前程序主要用于学习使用数组列表
     * @version 1.00
     * @author forfolja
     */
/*    public class ArrayListTest {
        public static void main(String[] args) {
            var staff = new ArrayList<Employee>();
            staff.add(new Employee("Carl Cracker", 75000, 1987, 12, 15));
            staff.add(new Employee("Harry Hacker", 50000, 1989, 10, 1));
            staff.add(new Employee("Tony Tester",40000,1990,3,15));

            for(Employee aEmployee:staff)
                aEmployee.raiseSalary(5);
            for(Employee aEmployee:staff)
                System.out.println(aEmployee.toString());


            System.out.println(staff.size());
            staff.remove(1);

            var bEmployee = new Employee("forfolja",3000000, 1985, 2, 2);
            staff.add(0,bEmployee);
            //staff.set(0,bEmployee);

            for(Employee aEmployee : staff)
                System.out.println("name=" + aEmployee.getName()+",salary="+aEmployee.getSalary()+",hireDay="+aEmployee.getHireDay());

            System.out.println(staff.size());
        }

}


 */